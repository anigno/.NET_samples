<Window x:Class="DispatcherExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dispatcherExample="clr-namespace:DispatcherExample"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance dispatcherExample:MainWindow}">
    <Grid>
        <StackPanel>
            <ListBox ItemsSource="{Binding MyCollection}" Width="400" Height="200" Background="LightBlue"/>
            <Label Width="200" Content="{Binding MyLabel}" Background="LightGray" Height="20"/>
        </StackPanel>
    </Grid>
</Window>
